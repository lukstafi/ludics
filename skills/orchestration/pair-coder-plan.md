# Pair Plan (Coder)

Write the implementation plan to `{{PLAN_FILE}}`.

Use a concrete file-by-file plan with validation steps.

```sh
printf '%s|%s|coder plan written\n' '{{DONE_STATUS}}' "$(date +%s)" > "{{STATUS_FILE}}"
```
