# Proposal: AC-rigor four-clauses round — heading-anchor + content-fingerprint pair, ls-probe AC-cited paths, window-scoped pairing, capture-and-feed mock-driven invariant

Task: `gh-ludics-495`

## Goal

Land four AC-rigor refinements in `docs/ac-rigor-reference.md` per resolved-question decisions, with `skills/worker-conventions.md` per-family list updates and preamble (clause-count) bump, coordinating with concurrent `task-4335d903` via the re-grep-and-bump protocol.

## Context

Filed by `/ludics-feedback-digest` consolidating four reviewer-pushback rounds (`task-96d69bf9`, `task-9cd6cdb9`, `task-a670cdbf`, `gh-ludics-479`) into 5 candidate clauses. All six task-elaboration questions resolved 2026-05-09:

- **Candidate #1 (symmetric-OR rule) is dropped** — `task-9120ddcf` was closed-as-done with the rule captured to `docs/swe-textbook.md`, deliberately *not* promoted to `ac-rigor-reference` per the competent-SWE filter. Promoting it here would contradict that decision. This proposal lands **4 clauses** (#2–#5), not 5; the filename keeps "four" to reflect the actual delta.
- **Candidate #2** (heading-anchor + content-fingerprint pair) → **extend** `### Vacuous doc/config harness — same rule, doc artifacts` with a sub-paragraph; sibling would duplicate principle.
- **Candidate #3** (`ls`-probe AC-cited test paths) → new sibling under **Verification-evidence family**; same shape as "evidence must survive commit boundary" / "diff-enumerated lines go stale" — the verification line is itself a load-bearing claim. Precipitated by `task-a670cdbf` round 2 (two non-existent test file citations: `src/adapters/t3code-orchestration-config.test.ts`, `src/mag-health-check-skill.test.ts`).
- **Candidate #4** ("Surfaces X as Y" window-scoped pairing) → new sibling under **Falsifier-shape family**, adjacent to `### Per-element assertions for enumerated-element ACs`. Pairing is structurally distinct from per-element (associations vs enumerations); regex-in-window is a different falsifier shape.
- **Candidate #5** (capture-and-feed mock-driven invariant) → new sibling under **Falsifier-shape family**. Prescription is a positive falsifier shape (mock → call → assert), not a vacuous-harness instance. Indirect-coverage citation ("covered by AC17 setup paths") was rejected for `transport-tmux.refreshAgentTransportState`'s raw-pane-capture AC in `task-a670cdbf` round 3.

Concurrent: `task-4335d903` resolved the same day, proposed at `docs/proposals/task-4335d903-ac-rigor-sub-round-3-refinements.md`. Both touch the same files. Doc currently has **17** `^### ` clauses (a `### Proposal-path enumeration goes stale...` clause landed since this issue's elaboration); preamble already says "seventeen". Use the same re-grep-and-bump protocol as `task-4335d903` AC8 — whichever lands first re-greps and bumps; the other follows. Doc-only proposal — orchestration plan phase skipped.

## Acceptance Criteria

1. **Candidate #2 sub-paragraph extension lands inside existing `### Vacuous doc/config harness — same rule, doc artifacts` clause body.** A "Per-AC clause: pair the two probes" sub-paragraph (or equivalent follow-on prose in the same `### ` body) prescribes that doc structural ACs require a heading-anchor probe **and** a content-fingerprint probe as a mandatory pair, with mutation = deleting either probe breaks at least one assertion. Falsifier: `grep -F 'Per-AC clause: pair the two probes' docs/ac-rigor-reference.md` returns ≥ 1 hit; `grep -F 'heading-anchor' docs/ac-rigor-reference.md` returns ≥ 1 hit; `grep -F 'content-fingerprint' docs/ac-rigor-reference.md` returns ≥ 1 hit. Adjacency: `awk '/^### Vacuous doc\/config harness/{f=1;next} f && /^### /{exit} f' docs/ac-rigor-reference.md | grep -F 'Per-AC clause: pair the two probes'` returns ≥ 1 hit (the new sub-paragraph sits in that clause body, not as a sibling — note: comma-range form `/^### X/,/^### /` is degenerate here per `awk-comma-range-degenerate-when-start-matches-close`, so the start-flag/exit idiom is required). **No new `### ` heading is added under `## Vacuous-harness family` for Candidate #2** (negative control: `awk '/^## Vacuous-harness family/{f=1;next} f && /^## /{exit} f' docs/ac-rigor-reference.md | grep -c '^### '` returns the same family count as before this proposal lands, modulo whatever `task-4335d903` lands separately).

2. **Candidate #3 new sibling clause lands under `## Verification-evidence family`.** A new `### ` clause titled exactly `### AC-cited test paths are load-bearing — ls-probe each before done-status` lands under `## Verification-evidence family`. Falsifier: `grep -Fn 'AC-cited test paths are load-bearing — ls-probe each before done-status' docs/ac-rigor-reference.md` returns exactly one hit; `awk '/^## Verification-evidence family/{f=1;next} f && /^## /{exit} f' docs/ac-rigor-reference.md | grep -F 'AC-cited test paths are load-bearing'` returns ≥ 1 hit (the new clause sits within the Verification-evidence family interval — start-flag/exit idiom required to avoid the degenerate comma-range form).

3. **Candidate #3 clause body prescribes `ls`-probe per AC-cited test path before signalling done, with the precipitating worked example.** The clause body (3–4 sentences) names `ls` (or equivalent file-existence probe) per AC-cited test path before done-status, frames the verification line itself as a load-bearing claim that must be probed, and cites the precipitating instance: `task-a670cdbf` round 2's two non-existent test paths (`src/adapters/t3code-orchestration-config.test.ts`, `src/mag-health-check-skill.test.ts`). Falsifier on the clause body interval (between `### AC-cited test paths are load-bearing — ls-probe each before done-status` and the next `### `): `grep -F 'ls'` returns ≥ 1 hit; `grep -F 'test path'` returns ≥ 1 hit; `grep -F 'task-a670cdbf'` returns ≥ 1 hit; `grep -F 't3code-orchestration-config.test.ts'` returns ≥ 1 hit; `grep -F 'mag-health-check-skill.test.ts'` returns ≥ 1 hit.

4. **Candidate #4 new sibling clause lands under `## Falsifier-shape family`, adjacent to `### Per-element assertions for enumerated-element ACs`.** A new `### ` clause titled exactly `### Window-scoped pairing assertion for "Surfaces X as Y" ACs` lands under `## Falsifier-shape family`, with no intervening `### ` heading between `### Per-element assertions for enumerated-element ACs` and the new clause's heading. Falsifier: `grep -Fn 'Window-scoped pairing assertion for "Surfaces X as Y" ACs' docs/ac-rigor-reference.md` returns exactly one hit; the line-number difference between `grep -nF 'Per-element assertions for enumerated-element ACs' docs/ac-rigor-reference.md` and `grep -nF 'Window-scoped pairing assertion for "Surfaces X as Y" ACs' docs/ac-rigor-reference.md` is positive (Window-scoped follows Per-element) and `awk -v from="<per-element-line>" -v to="<window-scoped-line>" 'NR>from && NR<to' docs/ac-rigor-reference.md | grep -c '^### '` returns 0 (no other `### ` clauses interleave). The comma-range form `/^### Per-element/,/^### /` is degenerate — start matches close on the heading line — so it is not used here.

5. **Candidate #4 clause body prescribes the slice-from-sentinel-to-next-top-level-marker probe shape with mutation = swap-two-halves-of-pairing, citing the precipitating instance.** The clause body articulates: extract a slice from a sentinel heading to the next top-level marker, regex-assert the pairing (severity ↔ event_type, action ↔ trigger) inside that slice; mutation test = swap the two halves of the pairing in the source. Cites `task-a670cdbf` round 3's `agent_hung_detected` ↔ "warning" pairing as the precipitating instance. Falsifier on the clause body interval: `grep -F 'slice'` returns ≥ 1 hit; `grep -F 'sentinel'` returns ≥ 1 hit; `grep -F 'swap'` returns ≥ 1 hit; `grep -F 'pairing'` returns ≥ 1 hit; `grep -F 'task-a670cdbf'` returns ≥ 1 hit.

6. **Candidate #5 new sibling clause lands under `## Falsifier-shape family`.** A new `### ` clause titled exactly `### Capture-and-feed ACs need a direct mock-driven invariant test, not indirect coverage` lands under `## Falsifier-shape family`. Falsifier: `grep -Fn 'Capture-and-feed ACs need a direct mock-driven invariant test, not indirect coverage' docs/ac-rigor-reference.md` returns exactly one hit; `awk '/^## Falsifier-shape family/{f=1;next} f && /^## /{exit} f' docs/ac-rigor-reference.md | grep -F 'Capture-and-feed ACs need a direct mock-driven invariant test'` returns ≥ 1 hit (start-flag/exit idiom required — the comma-range form opens and closes on the family heading).

7. **Candidate #5 clause body prescribes mock-producer / call-producer / assert-consumer-field shape with two-state invariant, citing the precipitating instance.** The clause body prescribes: mock the producer's I/O, call the producer, assert the consumer field updated to the mocked value; two-state invariant ("first-tick seeds X without seeding Y" / "under-threshold seeds Y AND refreshes X"). Cites the precipitating instance: `task-a670cdbf` round 3's `transport-tmux.refreshAgentTransportState` raw-pane-capture AC, where `tmuxCapture` is the mocked producer and `refreshAgentTransportState` is the called producer. Explicitly rejects indirect-coverage citation ("covered by AC17 setup paths"). Falsifier on the clause body interval: `grep -F 'mock'` returns ≥ 1 hit; `grep -F 'producer'` returns ≥ 1 hit; `grep -F 'tmuxCapture'` returns ≥ 1 hit; `grep -F 'refreshAgentTransportState'` returns ≥ 1 hit; `grep -F 'indirect coverage'` returns ≥ 1 hit; `grep -F 'first-tick'` returns ≥ 1 hit.

8. **Preamble's clause-count word is bumped at landing time via re-grep — do NOT pin a hardcoded number.** The implementer re-runs `grep -c '^### ' docs/ac-rigor-reference.md` against the working tree *before* writing the new clauses, computes `baseline + 3` (this proposal adds **+3** `### ` clauses; Candidate #2 is a sub-paragraph extension and contributes 0), and writes the corresponding English number in the preamble paragraph at line 5. Falsifier: `grep -F 'seventeen' docs/ac-rigor-reference.md` (the *previous* clause-count word) returns 0 hits in the preamble's count sentence (negative control — works whether the previous word was "seventeen", "eighteen", "nineteen", etc.; the implementer reads the actual previous word at landing time and confirms it no longer appears in the preamble's count sentence); `grep -cE '^### ' docs/ac-rigor-reference.md` returns the same number as the English clause-count word in the preamble (positive control — verify by grep'ing the English number in line 5). **No hardcoded number in this proposal**: the AC body explicitly forbids pinning a baseline count, because concurrent `task-4335d903` may land first or simultaneously and shift the baseline.

9. **`skills/worker-conventions.md` per-family list updated with the three new heading titles.** Under `## AC verification rigor`, the `### AC-cited test paths are load-bearing — ls-probe each before done-status` title joins the **Process-around-the-AC** bullet (which currently houses Verification-evidence family entries: `AC verification evidence must survive the commit boundary`, `Diff-enumerated verification lines go stale...`, `Proposal-path enumeration goes stale...`); the `### Window-scoped pairing assertion for "Surfaces X as Y" ACs` and `### Capture-and-feed ACs need a direct mock-driven invariant test, not indirect coverage` titles join the **Falsifier-shape family** bullet. Sub-paragraph extension to Candidate #2 does NOT require a worker-conventions touch — sub-paragraph extensions are not pointer entries. Falsifier: `grep -F 'AC-cited test paths are load-bearing — ls-probe each before done-status' skills/worker-conventions.md` returns ≥ 1 hit; `grep -F 'Window-scoped pairing assertion for "Surfaces X as Y" ACs' skills/worker-conventions.md` returns ≥ 1 hit; `grep -F 'Capture-and-feed ACs need a direct mock-driven invariant test, not indirect coverage' skills/worker-conventions.md` returns ≥ 1 hit; all three literal occurrences sit in the bulleted list block under `## AC verification rigor`. Negative control: `grep -F 'Per-AC clause: pair the two probes' skills/worker-conventions.md` returns 0 hits (sub-paragraph extension is not a pointer entry).

10. **Coordination with `task-4335d903` via re-grep-and-bump protocol.** The implementer of either task checks the other's status before landing. If `task-4335d903` has already merged, this task re-runs `grep -c '^### ' docs/ac-rigor-reference.md` against the post-merge working tree and bumps the preamble's clause-count word from *that* baseline + 3. If this task lands first, `task-4335d903`'s implementer follows the same re-grep-and-bump protocol (its AC8 already names this protocol). The proposal text (this file) explicitly states this protocol and names `task-4335d903` by id, referencing its proposal path. Falsifier on the proposal text: `grep -F 'task-4335d903' docs/proposals/gh-ludics-495-ac-rigor-four-clauses-round.md` returns ≥ 1 hit; `grep -F 're-grep' docs/proposals/gh-ludics-495-ac-rigor-four-clauses-round.md` returns ≥ 1 hit; `grep -F 'docs/proposals/task-4335d903-ac-rigor-sub-round-3-refinements.md' docs/proposals/gh-ludics-495-ac-rigor-four-clauses-round.md` returns ≥ 1 hit.

## Approach

- **Pinned heading texts** (use these literals verbatim — they are AC-load-bearing):
  - Candidate #3: `### AC-cited test paths are load-bearing — ls-probe each before done-status`
  - Candidate #4: `### Window-scoped pairing assertion for "Surfaces X as Y" ACs`
  - Candidate #5: `### Capture-and-feed ACs need a direct mock-driven invariant test, not indirect coverage`
- **Pinned sub-paragraph distinguishing literals for Candidate #2**: `Per-AC clause: pair the two probes`, `heading-anchor`, `content-fingerprint`. The sub-paragraph is appended to the existing `### Vacuous doc/config harness — same rule, doc artifacts` clause body — no new `### ` for it.
- **Pinned worked-example references** (clause bodies must cite these so reviewers can follow back to the precipitating round):
  - Candidate #3 body: `task-a670cdbf` round 2; non-existent test paths `src/adapters/t3code-orchestration-config.test.ts`, `src/mag-health-check-skill.test.ts`.
  - Candidate #4 body: `task-a670cdbf` round 3; `agent_hung_detected` ↔ "warning" pairing.
  - Candidate #5 body: `task-a670cdbf` round 3; `transport-tmux.refreshAgentTransportState` / `tmuxCapture`; rejected indirect-coverage citation "covered by AC17 setup paths".
- **Pinned clause-count delta**: +3 `^### ` clauses (Candidate #2 is sub-paragraph, no delta). Compute the new preamble English number as `current(grep -c '^### ' docs/ac-rigor-reference.md) + 3` against the working tree at landing time. **Do not hardcode** — concurrent `task-4335d903` may shift the baseline.
- **Pinned worker-conventions touches**: append three heading titles to matching family bullets under `## AC verification rigor` in `skills/worker-conventions.md` (Candidate #3 → Process-around-the-AC bullet; Candidates #4 and #5 → Falsifier-shape family bullet). No reordering of existing entries; no new bullet lines. Sub-paragraph extension for Candidate #2 has no worker-conventions touch.
- **Coordination protocol** (re-grep-and-bump): the second task to land re-runs `grep -c '^### ' docs/ac-rigor-reference.md` and updates the preamble's English clause-count word from the post-merge baseline. Both proposals' bumping ACs (this file's AC8 + `task-4335d903`'s AC8) use the same protocol so race-coherent landings produce a coherent preamble count.
- **Editorial pattern** (per prior rounds — `task-d6656cf3`, `task-9cd6cdb9`): clause body 3–5 sentences; opens with the rule; names source task/PR; gives worked-example signature; cites the mutation test that catches a regression in the rule itself. No cross-link decoration is required for this round — the four clauses don't pair with each other in the way Stash-prod ↔ No-regression do, so no `## See also` lines beyond what the clauses' own bodies cite.

## Out of scope

- **Symmetric-OR rule (Candidate #1) — dropped per textbook capture and competent-SWE filter.** `task-9120ddcf` closed-as-done captured this to `docs/swe-textbook.md`; promoting it here would contradict that decision. Re-promoting requires a separate task and explicit user override of the competent-SWE filter.
- **Structural pinning tests for `docs/ac-rigor-reference.md`** in `src/orchestration/skills.test.ts` (rejected by `task-4335d903`'s Q5 — same scope discipline; verification stays per-PR `grep -F` snippet probes).
- **Reviewer-prompt notes** — no edits to reviewer skill templates this round.
- **Any other AC-rigor clause additions outside Candidates #2–#5** — the issue body's five-candidate enumeration is the contract surface; new patterns from later rounds are separate tasks.
- **`task-4335d903`'s three refinements** — intentionally a separate proposal (different precipitating retros, different editorial decisions: self-contradicting-AC enforceability, set-equality for closed-set, sibling-mutation for cardinality). `gh-ludics-495`'s clauses must not pre-empt those clauses; coordinate via AC10's re-grep-and-bump protocol.
- **Refactoring or reordering existing `### ` clauses or `## ` families.**
- **Editing `docs/orchestration-patterns.md` or `docs/swe-textbook.md`** — cross-references, if any, are one-way (this round's clauses don't add cross-link decoration).
